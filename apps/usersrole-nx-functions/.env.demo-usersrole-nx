# Loaded only when the functions run against the demo-usersrole-nx project,
# which exists only inside the local Emulator Suite. Sign-up is open to the
# reserved example.com domain so the emulator can be tried end to end.
SIGNUP_ALLOWED_EMAILS=@example.com
