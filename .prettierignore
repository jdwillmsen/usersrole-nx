# Add files here to ignore them from prettier formatting
/dist
/coverage
.angular

/.nx/cache
/.nx/workspace-data
.nx/self-healing

# Regenerated at build time with JSON-style quoting; keep prettier away.
libs/usersrole-nx-app/main/src/lib/generated/version-info.ts

# Written by release-please on every release PR; its markdown doesn't match
# prettier's, and format:check would fail the required CI check on that PR.
CHANGELOG.md
